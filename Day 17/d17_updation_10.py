# how to update the tuple
# kindly note down

s = 'this is sample'
s[0] = '0'

# so strings are not possbile to update because it is immutable

t = (4,5,6,7)
t[0] = 34	# so tuples are also not possbile to update because it is immutable

print(t)